mixin skills-and-services-screen__skills-cards(modifier)
  if (modifier["skillCards"] == undefined)
    - modifier["skillCards"] = [];
  ul.skills-and-services-screen__skills-cards&attributes(attributes)
    each skillCard in modifier.skillCards
      if skillCard["modification"] == undefined
        - skillCard["modification"] = "";
      li
        +skill-card(skillCard)(class=`skills-and-services-screen__skill-card ${skillCard.modification}`)